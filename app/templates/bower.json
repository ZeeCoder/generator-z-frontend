{
  "name": "project",

  "//": "These are dependencies which will be installed under the bower/ directory in the project's public directory via the `gulp bower` command.",
  "dependencies": {
    "jquery": "~2.1.0"
  },

  "//": "These are dependencies that will only be used when building the assets.",
  "devDependencies": {
    "clam": "0.3.0-alpha1",
    "normalize-scss": "~3.0.2",
    "bourbon": "~3.1.8",
    "sass-mediaqueries": "~1.4.0"
  },

  "//": {
    "the clam dependency": "The clam dependency should be updated as soon as a stable version is available.",
    "often used deps": {
      "ckeditor": "~4.4.6",
      "sweetalert": "~0.4.1",
      "typed.js": "*",
      "jquery-autosize": "~1.18.17",
      "modernizr": "~2.8.3",
      "foundation": "~5.5.0",
      "select2": "~3.5.2",
      "angularjs": "1.3.11"
    },
    "example overrides (Used by `gulp bower`)": {
      "ckeditor": {
        "main": "**/*"
      },
      "modernizr": {
        "main": [
          "modernizr.js"
        ]
      }
    }
  }
}
